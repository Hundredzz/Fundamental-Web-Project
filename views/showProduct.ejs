<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('head') %>
        <title>Product Page</title>
        <link rel="stylesheet" href="/css/showProduct.css">
        <script src="/script/productPopUp.js" defer></script>
        <script src="/script/productHandling.js" defer></script>
</head>

<body>
    <%- include('nav') %>
        <div id="main-content" class="main-content">
            <div>
                <input type="text" name="search" placeholder="Search products..." id="search-text" onkeydown="if(event.key === 'Enter') changePage(this)">
                <button type="submit" onclick="changePage(this)">Search</button>
                <select id="category-select" onchange="changePage(this)">
                    <option value="">ประเภทสินค้า</option>
                    <% categories.forEach(category=> { %>
                        <option value="<%= category.category_name %>">
                            <%= category.category_name %>
                        </option>
                    <% }) %>
                </select>

                <select id="brand-select" onchange="changePage(this)">
                    <option value="">แบรนด์สินค้า</option>
                    <% brands.forEach(brand=> { %>
                        <option value="<%= brand.brand_name %>">
                            <%= brand.brand_name %>
                        </option>
                    <% }) %>
                </select>
                <button onclick="window.location.href='/addProduct'">+ เพื่มสินค้า</button>
            </div>
            <div id="product-sect">
                <%- include('productSection', { data: data, currentPage: currentPage, totalPages: totalPages }) %>
            </div>
        </div>
        <%- include('productPopup') %>

</body>

</html>